app:
  name: customers-service
  logger: main

container:
  type: dig

http-servers:
  instances:
    customers-service:
      type: echo
      host: 127.0.0.1
      port: 8001
      debug: true
      logger: main-http-server

rpc-servers:
  instances:
    customers-service:
      type: grpc
      host: 127.0.0.1
      port: 9001
      debug: true
      logger: main-rpc-server

database-clients:
  instances:
    customers-service:
      type: mongodb
      host: localhost
      port: 27017
      user: admin
      password: admin
      database: customers-service
      sslMode: false

loggers:
  type: logrus
  level: debug
  instances:
    main:
      prefix: customers-service
    main-http-server:
      prefix: customers-service (http-server)
    main-rpc-server:
      prefix: customers-service (rpc-server)

migrations:
  instances:
    customers-service:
      db: customers-service
      migrationsDir: db/migrations
